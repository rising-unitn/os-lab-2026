man ls
# -S: sort by file size, largest first
ls -lS /etc

type cd      # cd is a shell builtin
type echo    # echo is a shell builtin
type cat     # cat is /usr/bin/cat (external command)

which ls
# Typically: /usr/bin/ls

man 1 passwd   # the command
man 5 passwd   # the file format (/etc/passwd)
